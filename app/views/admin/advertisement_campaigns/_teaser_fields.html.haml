.nested-fields
  = f.input :text
  - if f.object.image?
    = image_tag f.object.image.thumb_200
    = f.input :image, required: false
  - else
    = f.input :image
  = link_to_remove_association "remove task", f