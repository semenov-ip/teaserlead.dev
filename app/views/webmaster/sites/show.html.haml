- model_class = Site
.page-header
  %h1=t '.title', :default => model_class.model_name.human.titleize

%p
  %strong= model_class.human_attribute_name(:category_id) + ':'
  %br
  = @site.category_id
%p
  %strong= model_class.human_attribute_name(:title) + ':'
  %br
  = @site.title
%p
  %strong= model_class.human_attribute_name(:url) + ':'
  %br
  = @site.url
%p
  %strong= model_class.human_attribute_name(:description) + ':'
  %br
  = @site.description

.form-actions
  = link_to t('.back', :default => t("helpers.links.back")), webmaster_sites_path, :class => 'button secondary'
  = link_to t('.edit', :default => t("helpers.links.edit")), edit_webmaster_site_path(@site), :class => 'button secondary'
  = link_to t('.destroy', :default => t("helpers.links.destroy")), webmaster_site_path(@site), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'button alert'
